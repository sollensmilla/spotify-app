{
  "name": "spotify-app",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix auth-server\"",
    "lint": "eslint frontend/ auth-server/",
    "lint:fix": "eslint frontend/ auth-server/ --fix"
  },
  "devDependencies": {
    "@lnu/eslint-config": "^1.1.10",
    "autoprefixer": "^10.5.0",
    "concurrently": "^9.2.1",
    "eslint": "^8.57.1"
  }
}
