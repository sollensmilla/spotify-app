{
  "name": "spotify-app",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix auth-server\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}